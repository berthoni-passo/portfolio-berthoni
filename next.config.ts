import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configuration Next.js
  // Note: Le bouton dev en bas à gauche se ferme en cliquant dessus ou via l'option React DevTools.
  // Depuis Next.js 15+, il ne peut plus être totalement retiré par configuration.
};

export default nextConfig;
